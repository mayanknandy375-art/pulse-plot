# PulsePlot

Deployment-ready single-service website.

## Render settings
- Runtime: Node
- Build Command: `npm install`
- Start Command: `npm start`

The service serves the website and its Socket.IO real-time channel from the same public URL.
